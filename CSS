/* Resetting default margin and padding */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Global styles */
body {
  font-family: Arial, sans-serif;
  background-color: #ffffff; /* Primary color */
  color: #000000; /* Black text color */
}

.container {
  max-width: 1200px;
  margin: 0 auto; /* Centering content */
  padding: 0 20px; /* Adding some padding */
}

/* Header styles */
.header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  background-color: #ffff00; /* Secondary color */
  z-index: 1000; /* Ensuring header is on top */
}

/* Menu styles */
.menu {
  background-color: #ffff00; /* Secondary color */
}

/* Hero section styles */
.hero {
  background: linear-gradient(to right, #ffffff, #ffff00); /* Gradient color */
  padding: 100px 0;
}

/* New courses section styles */
.new-courses {
  padding: 80px 0;
}

/* Featured courses section styles */
.featured-courses {
  padding: 80px 0;
}

/* Education certificate section styles */
.education-certificate {
  padding: 80px 0;
}

/* Inquiry form section styles */
.inquiry-form {
  padding: 80px 0;
}

/* About us section styles */
.about-us {
  padding: 80px 0;
}

/* Contact us section styles */
.contact-us {
  padding: 80px 0;
}

/* Footer styles */
.footer {
  background-color: #ffff00; /* Secondary color */
  padding: 40px 0;
}

/* Responsive styles */
@media only screen and (max-width: 768px) {
  .container {
    padding: 0 15px; /* Adjusting padding for smaller screens */
  }
}
